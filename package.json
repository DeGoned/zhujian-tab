{
  "name": "zhujian-tab",
  "version": "1.1.0",
  "private": true,
  "description": "zhujian-tab — Chrome extension: tab dashboard + todos + Mac notifications. Fork of tab-out. Dev-only npm; extension itself has zero runtime deps.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0",
    "jest-chrome": "^0.8.0"
  }
}
